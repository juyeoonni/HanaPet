<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kopo.finalproject.product.model.dao.ProductMapper">
    <!-- 상품 전체 조회 -->
    <select id="getAllProduct" parameterType="com.kopo.finalproject.product.model.dto.Product"
            resultType="com.kopo.finalproject.product.model.dto.Product">
        SELECT *
        FROM pet_saving_product
    </select>

    <!-- 로그인 -->
    <!--    <select id="loginGuest" parameterType="HashMap" resultType="com.kopo.finalproject.guest.model.dto.Guest">-->
    <!--        SELECT-->
    <!--            *-->
    <!--        FROM guest-->
    <!--        where guest_id = #{guest_id}-->
    <!--          and pw = #{pw}-->
    <!--    </select>-->

</mapper>